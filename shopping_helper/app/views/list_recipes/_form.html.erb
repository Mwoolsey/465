<%= form_for([@list,@list_recipe]) do |f| %>
  <% if @list_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list_recipe.errors.count, "error") %> prohibited this list_recipe from being saved:</h2>

      <ul>
      <% @list_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :recipe_id, @list.recipes_not_used %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
